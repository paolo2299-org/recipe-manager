services:
  recipe-manager:
    image: ${IMAGE_NAME:-recipe-manager}:${IMAGE_TAG:-main}
    env_file:
      - ${ENV_FILE:-.env}
    environment:
      # DATABASE_PATH defaults to /app/data/recipes.db (set in the Dockerfile).
      PORT: 8080
